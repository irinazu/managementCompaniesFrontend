import { ServiceDescription } from './service-description';

describe('ServiceDescription', () => {
  it('should create an instance', () => {
    expect(new ServiceDescription()).toBeTruthy();
  });
});
