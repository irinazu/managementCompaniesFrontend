import { RepairWorkForSend } from './repair-work-for-send';

describe('RepairWorkForSend', () => {
  it('should create an instance', () => {
    expect(new RepairWorkForSend()).toBeTruthy();
  });
});
